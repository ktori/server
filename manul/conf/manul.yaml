manul:
  apps:
    - name: Manul
      type: default
      server:
        listeners:
          - type: http
            port: 8080
          - type: https
            port: 8081
            cert: conf/ssl/cert.pem
            key: conf/ssl/key.pem
